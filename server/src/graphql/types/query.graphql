type Query {
  peers: [String!]
  balance: String
  address: String
  block(hash: String!): Block
  blockchain: [Block]
  transaction(id: String!): Transaction
  transactionPool: [Transaction]
  measurementPool: [Measurement]
  unspentTransactionOutputs(address: String): [UnspentTxOut]
  myUnspentTransactionOutputs: [UnspentTxOut]
}
