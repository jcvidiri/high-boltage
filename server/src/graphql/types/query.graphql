type Query {
  peers: [String!]
  balance(address: String): String
  allBalances: [Balance]
  address: Address
  block(hash: String!): Block
  blockchain: [Block]
  flowPool: [Flow]
  contractPool: [RawContract]
  status: String
}
