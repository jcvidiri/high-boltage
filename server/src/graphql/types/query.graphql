type Query {
  peers: [String!]
  balance(address: String): String
  allBalances: [Balance]
  address: String
  block(hash: String!): Block
  blockchain: [Block]
  flowPool: [Flow]
  contractPool: [RawContract]
}
