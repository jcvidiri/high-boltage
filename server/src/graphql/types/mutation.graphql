type Mutation {
  startMinting: Minting
  stopMinting: Minting
  addFlow(flow: FlowInput!): Flow
  createFlow(flow: CreateFlowInput!): Flow
  createContract(contract: ContractInput!): RawContract
  setLogs(logs: Boolean!): Boolean
}
